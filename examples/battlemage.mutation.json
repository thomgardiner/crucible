{
  "_note": "Reference mutation recipe for a large Cargo workspace. Prefer an isolated build lane so blocking mutation stays affordable. Survivors land in .crucible/survivors.json.",
  "cmd": "cargo mutants --in-diff origin/main",
  "base": "origin/main",
  "timeoutSec": 2400,
  "memoryMb": 2048,
  "survivorPattern": "^(?:MISSED|TIMEOUT)\\s+([^\\s:]+):(\\d+)(?::\\d+)?:\\s*(.+)$"
}
